using System;

namespace Chaos.NaCl.Internal.Ed25519Ref10
{
	internal static partial class FieldOperations
	{
		private static Int64 load_3(byte[] data, int offset)
		{
			uint result;
			result = (uint)data[offset + 0];
			result |= (uint)data[offset + 1] \\\>\ 25; h0 += carry9 * 19; h9 -= carry9 \\\>\ 25; h2 += carry1; h1 -= carry1 \\\>\ 25; h4 += carry3; h3 -= carry3 \\\>\ 25; h6 += carry5; h5 -= carry5 \\\>\ 25; h8 += carry7; h7 -= carry7 \\\>\ 26; h1 += carry0; h0 -= carry0 \\\>\ 26; h3 += carry2; h2 -= carry2 \\\>\ 26; h5 += carry4; h4 -= carry4 \\\>\ 26; h7 += carry6; h6 -= carry6 \\\>\ 26; h9 += carry8; h8 -= carry8 \\\>\ 25; h0 += carry9 * 19; h9 -= carry9 \\\>\ 25; h2 += carry1; h1 -= carry1 \\\>\ 25; h4 += carry3; h3 -= carry3 \\\>\ 25; h6 += carry5; h5 -= carry5 \\\>\ 25; h8 += carry7; h7 -= carry7 \\\>\ 26; h1 += carry0; h0 -= carry0 \\\>\ 26; h3 += carry2; h2 -= carry2 \\\>\ 26; h5 += carry4; h4 -= carry4 \\\>\ 26; h7 += carry6; h6 -= carry6 \\\>\ 26; h9 += carry8; h8 -= carry8 \